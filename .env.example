# Copy this file to .env and fill in your token.
# A GitHub personal access token is optional but recommended to avoid rate limiting.
# Required scopes: none (public repos only) or "repo" for private repositories.
GITHUB_TOKEN=your_github_token_here
